- ¿Hay alguna forma global de meter bootstrap para todas las páginas? ¿Desde styles/global.css importandolo?
- ¿Por qué mete el repository dentro de view?
- Fichero config.php.
- Para que me sirve el util?.

-- 

- ?controller=¿nombreClase o nombreFichero?
- ¿Cómo recibe el index los parámetros controller y action?
- Repository --> Para interactuar con la BBDD.
- ¿Patrón de diseño singleton?

-- bindValue VS bindParam --
 · bindValue -> binds a value to a parameter.
 · bindParam -> binds a parameter to a  specified variable by reference.
 Es decir, si cambio el valor de la variable, con param permite el cambio, pero con value seguirá metiendo el valor que le fue asignado en primer lugar.


 ## Key Differences: ##
· Binding Type:
    - bindParam binds a variable by reference.
    - bindValue binds a value directly.
· Evaluation Time:
    - bindParam evaluates the variable's value at the time of the execute call.
    - bindValue evaluates the variable's value at the time of the bindValue call.
· Use Cases:
    - bindParam is useful for executing the same prepared statement multiple times with different values.
    - bindValue is useful for binding a fixed value that should not change between executions.



1º-> modelo.
2º-> repository.
2.5º -> metodoPDO private en repository.
3º-> añadir requires de model y bd en repository.
4º-> consulta/s necesaria/s en el repository. -- Preguntar a cipri el while con fetch en la consulta. Lo comentó ayer 25/03 en clase -- 
 -- ¿Cómo funciona exactamente el foreach?
 5º -> listado. si lo hay.
 6º -> controller.
 7º-> una vez que se ha loggeado->varSesion logged = true.
 -- Cuidado al redirigir desde el UserController.
 -- ¿Puedo establecer que BASE_URL sea una pagina por defecto? Es decir, partiendo de que todas las rutas posibles salgan de mi BASE_URL, establecer que esta sea como una página de inicio. En este caso el login como lo aplico? o que a baseURL sea el login + BASE_URL. 
 -- 


-- PARA INSERTAR --
1º-> En el header, redirijo hacia el metodo del controller que carga el formulario.
2º-> Desde el action del formulario, redirijo hacia el método del controller que inserta en la BBDD.
3º-> En este último método. Debo de settear los campos en un nuevo objeto de tipo repositorio, para luego llamar al método del repositorio y pasarle el objeto que hemos creado.
4º-> En el htaccess, las únicas dos rutas con respecto a insertar son:
        * La ruta en el header que redirecciona al formulario.
        * La ruta en el formulario que llama al método del controller para hacer la consulta.

                    
